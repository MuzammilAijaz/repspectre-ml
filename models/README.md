# Purpose
Collection of data to classify whether a barbell lift has started.
so its a binary classification : "lift" and "no-lift"

# Methodology
-> collection of different "sub-classes" (like slow/fast versions of the lift) of data to have variety in dataset
-> collection of different types of noise.

for more information on hierarchy and details of each sub-class of the dataset, see Dataset.md
