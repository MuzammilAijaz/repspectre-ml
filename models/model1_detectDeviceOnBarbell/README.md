# How to train a model
use organizeSqlDataIntoFiles.ipynb to extract data into csv into the raw_data folder
use data_split to convert data into train/test/val folders inside the data folder.
use train.py with --model parameter (LSTM or CNN) to train the model.

